# Dev share

First you need to be autenticated with the Github CLI.(this is actually optional)

```ssh
gh auth login
```

2. Need to get a personal access token from the web service.

3. Create a file called `.env` in the root of the project and add the following:

